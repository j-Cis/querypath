# plot-archive_2026Q3D262W38_Sat19Sep_180709623_M.md

```plaintext
[KiB 9.316] └──┬ 📂 querypath                         ./querypath/
[  B 657.0]    ├──• ⚙️ Cargo.toml                    ./Cargo.toml
[KiB 8.649]    ├──┬ 📂 src                            ./src/
[KiB 8.210]    │  ├──┬ 📂 code                        ./src/code/
[  B 66.00]    │  │  ├──• 🦀 fs_walk.rs               ./src/code/fs_walk.rs
[KiB 7.917]    │  │  ├──• 🦀 paths_entry.rs           ./src/code/paths_entry.rs
[  B 65.00]    │  │  ├──• 🦀 paths_options.rs         ./src/code/paths_options.rs
[  B 68.00]    │  │  ├──• 🦀 paths_patterns.rs        ./src/code/paths_patterns.rs
[  B 101.0]    │  │  └──• 🦀 querypath.rs             ./src/code/querypath.rs
[  B 449.0]    │  └──• 🦀 lib.rs                      ./src/lib.rs
[  B 26.00]    └──┬ 📂 tests                          ./tests/
[  B     0]       ├──┬ 📂 _                           ./tests/_/
[  B     0]       │  ├──┬ 📂 .gh                      ./tests/_/.gh/
[  B     0]       │  │  ├──• 📄 .oiu                  ./tests/_/.gh/.oiu
[  B     0]       │  │  ├──• 📄 aoe                   ./tests/_/.gh/aoe
[  B     0]       │  │  ├──┬ 📂 lib                   ./tests/_/.gh/lib/
[  B     0]       │  │  │  ├──┬ 📂 dw                 ./tests/_/.gh/lib/dw/
[  B     0]       │  │  │  │  ├──┬ 📂 dwew            ./tests/_/.gh/lib/dw/dwew/
[  B     0]       │  │  │  │  │  └──┬ 📂 src          ./tests/_/.gh/lib/dw/dwew/src/
[  B     0]       │  │  │  │  │     └──• 📄 erw.wq    ./tests/_/.gh/lib/dw/dwew/src/erw.wq
[  B     0]       │  │  │  │  ├──• 🦀 wewe.rs         ./tests/_/.gh/lib/dw/wewe.rs
[  B     0]       │  │  │  │  └──• 📄 wxw             ./tests/_/.gh/lib/dw/wxw
[  B     0]       │  │  │  └──• 🦀 jewe.rs            ./tests/_/.gh/lib/jewe.rs
[  B     0]       │  │  ├─── 📂 src                   ./tests/_/.gh/src/
[  B     0]       │  │  └──• 🦀 wrt.rs                ./tests/_/.gh/wrt.rs
[  B     0]       │  ├──┬ 📂 .ijk                     ./tests/_/.ijk/
[  B     0]       │  │  ├─── 📂 lib                   ./tests/_/.ijk/lib/
[  B     0]       │  │  └─── 📂 src                   ./tests/_/.ijk/src/
[  B     0]       │  ├──┬ 📂 abc                      ./tests/_/abc/
[  B     0]       │  │  ├─── 📂 mod                   ./tests/_/abc/mod/
[  B     0]       │  │  └─── 📂 src                   ./tests/_/abc/src/
[  B     0]       │  └──┬ 📂 def                      ./tests/_/def/
[  B     0]       │     ├─── 📂 app                   ./tests/_/def/app/
[  B     0]       │     └─── 📂 src                   ./tests/_/def/src/
[  B 26.00]       └──• 🦀 test_paths_entry.rs         ./tests/test_paths_entry.rs
```

### 001: `./Cargo.toml`

```toml
[package]
name = "querypath"
version = "0.1.0-dev.1"
authors = ["Jan Roman Cisowski <code@cisowscy.com>"]
description = ""
documentation = ""
homepage = ""
repository = "https://github.com/j-Cis/querypath"
readme = "README.md"
keywords = ["directory", "recursive", "walk", "iterator"]
categories = ["filesystem"]
license = "Unlicense/MIT"
edition = "2024"
rust-version = "1.96.0"
resolver = "3"

[package.metadata.cargo]
edition = "2024"

[lib]
name = "querypath"
path = "src/lib.rs"

[badges]
travis-ci = { repository = "j-Cis/querypath" }
appveyor = { repository = "j-Cis/querypath" }

[dependencies]
anyhow = "1.0.104"
regex = "1.13.1"
walkdir = "2.5.0"

```

### 002: `./src/code/fs_walk.rs`

```rust
// ./src/code/fs_walk.rs
// przechodzenie po katalogach i plikach
```

### 003: `./src/code/paths_entry.rs`

```rust
// ./src/code/paths_entry.rs

use std::env;
use std::fs;
use std::path::{Path, PathBuf};
use anyhow::{Context, Result, bail};

// ============================================================================
// NORMALIZACJA ŚCIEŻEK
// ============================================================================

/// Normalizuje ścieżkę do ujednoliconego formatu tekstowego (POSIX-like).
/// - Zmienia `\` na `/`
/// - Usuwa Windowsowe prefiksy UNC (`\\?\`)
/// - Redukuje zduplikowane sekwencje `/./` do `/`
#[must_use]
pub fn normalize_path<P: AsRef<Path>>(p: P) -> String {
    p.as_ref()
        .to_string_lossy()
        .trim_start_matches(r"\\?\")
        .replace('\\', "/")
        .replace("/./", "/")
}

// ============================================================================
// BAZOWE STRUKTURY DANYCH (DOMENA)
// ============================================================================

/// Reprezentacja pojedynczego węzła ścieżki przechowująca zarówno
/// oryginalny obiekt systemowy (`PathBuf`), jak i szybką, znormalizowaną
/// postać tekstową (`String`).
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct PathNode {
    pub buf: PathBuf,
    pub str: String,
}

impl PathNode {
    pub fn new(buf: PathBuf) -> Self {
        let str = normalize_path(&buf);
        Self { buf, str }
    }
}

/// ============================================================================
/// PATH CONTEXT (ZERO-COPY / STACK-BASED)
/// ============================================================================
/// Analiza relacji wewnątrz ścieżki tekstowej bez ani jednej alokacji na stercie.
/// Działa w 100% na pożyczonych wycinkach referencji (`&str`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct PathContext<'a> {
    pub parent: &'a str,
    pub file: &'a str,
}

impl<'a> PathContext<'a> {
    /// Dzieli ścieżkę na katalog nadrzędny i plik/katalog docelowy.
    #[must_use]
    pub fn from_path(path: &'a str) -> Self {
        let clean_path = path.trim_start_matches("./");

        let (parent, file) = match clean_path.rfind('/') {
            Some(idx) => (&clean_path[..idx], &clean_path[idx + 1..]),
            None => ("", clean_path),
        };

        Self { parent, file }
    }

    pub fn name(&self) -> &'a str {
        self.file
    }

    pub fn parent(&self) -> &'a str {
        self.parent
    }

    pub fn is_root_level(&self) -> bool {
        self.parent.is_empty()
    }
}

// ============================================================================
// RELACJE I ZAKOTWICZENIE (ANCHORING)
// ============================================================================

/// Odwzorowanie relacji pomiędzy miejscem odpalenia programu (CWD),
/// a fizycznym celem skanowania, wyliczające zawsze bezpieczną ścieżkę relatywną.
#[derive(Debug, Clone)]
pub struct AnchoredPath {
    pub execution_dir: PathNode, // Katalog, z którego wywołano aplikację (CWD)
    pub target_dir: PathNode,    // Fizyczna lokalizacja poddawana skanowaniu
    pub relative_path: String,   // Ścieżka pomostowa między nimi
}

impl AnchoredPath {
    /// Tworzy nowy punkt zakotwiczenia. Zwraca błąd, jeśli ścieżka docelowa 
    /// nie istnieje na dysku (wczesna walidacja z kontekstem).
    pub fn resolve<P: AsRef<Path>>(target: P, cwd_node: &PathNode) -> Result<Self> {
        let target_ref = target.as_ref();

        // Kanonizacja rozwija m.in. symlinki, '..' i weryfikuje istnienie na dysku.
        let absolute_target = fs::canonicalize(target_ref)
            .with_context(|| format!("Ścieżka docelowa nie istnieje lub jest niedostępna: {:?}", target_ref))?;

        let target_node = PathNode::new(absolute_target);

        // Wyliczamy relację (jak dostać się z CWD do Targetu).
        // Jeśli target jest na innym dysku (Windows), staje się ścieżką absolutną.
        let relative_path = match target_node.buf.strip_prefix(&cwd_node.buf) {
            Ok(rel) => {
                let s = rel.to_string_lossy().replace('\\', "/");
                if s.is_empty() {
                    "./".to_string()
                } else {
                    format!("./{}/", s)
                }
            }
            Err(_) => normalize_path(&target_node.buf),
        };

        Ok(Self {
            execution_dir: cwd_node.clone(),
            target_dir: target_node,
            relative_path,
        })
    }
}

// ============================================================================
// GŁÓWNY PUNKT WEJŚCIA I ORKIESTRACJA WEJŚĆ (ENTRY MANAGER)
// ============================================================================

/// Menadżer zarządzający wszystkimi lokalizacjami startowymi skanowania.
/// Rozwija alternatywy `{a|b}` oraz weryfikuje ich istnienie.
#[derive(Debug, Clone)]
pub struct PathsEntry {
    pub execution_dir: PathNode,
    pub targets: Vec<AnchoredPath>,
}

impl PathsEntry {
    /// Inicjuje listę celów skanowania na podstawie podanych przez użytkownika stringów.
    /// Jeśli lista jest pusta, domyślnie używa obecnego katalogu roboczego (`.`).
    pub fn build<I, S>(inputs: I) -> Result<Self>
    where
        I: IntoIterator<Item = S>,
        S: AsRef<str>,
    {
        // 1. Ustalenie bezpiecznego CWD dla całej aplikacji.
        let cwd_buf = env::current_dir()
            .context("Nie można odczytać katalogu roboczego (CWD)")?;
        let cwd_node = PathNode::new(cwd_buf);

        let mut raw_inputs: Vec<String> = inputs.into_iter().map(|s| s.as_ref().to_string()).collect();

        // 2. Jeśli nie podano żadnej ścieżki - domyślnie badamy CWD
        if raw_inputs.is_empty() {
            raw_inputs.push("./".to_string());
        }

        let mut expanded_paths = Vec::new();
        for input in raw_inputs {
            expanded_paths.extend(Self::expand_alternatives(&input));
        }

        // 3. Budowanie punktów zakotwiczenia.
        // Ciche ignorowanie niedziałających wariantów (np. gdy z `./{src|nonexistent}/`
        // rozwinie się `nonexistent`, pominiemy go, o ile coś innego przetrwa).
        let mut targets = Vec::new();
        for expanded in expanded_paths {
            if let Ok(anchored) = AnchoredPath::resolve(&expanded, &cwd_node) {
                // Zapobiegamy duplikatom w przypadku nadmiarowych wpisów użytkownika
                if !targets.iter().any(|t: &AnchoredPath| t.target_dir == anchored.target_dir) {
                    targets.push(anchored);
                }
            }
        }

        // Jeśli po rozwiązaniu alternatyw nie mamy NICZEGO do przeskanowania, zgłaszamy błąd.
        if targets.is_empty() {
            bail!("Żadna z podanych lub rozwiniętych ścieżek wejściowych nie istnieje na dysku.");
        }

        Ok(Self {
            execution_dir: cwd_node,
            targets,
        })
    }

    /// Rekurencyjnie rozwija alternatywne bloki zapisane w klamrach, 
    /// np.: `./{abc/ao/|def/}` -> `./abc/ao/` oraz `./def/`
    /// Wspiera rozdzielanie zarówno znakiem `|` (z specyfikacji), jak i `,` (posix).
    fn expand_alternatives(input: &str) -> Vec<String> {
        // Szukamy najgłębiej zagnieżdżonej prawej klamry
        let Some(end) = input.find('}') else {
            return vec![input.to_string()];
        };

        // Szukamy najbliższej lewej klamry przed nią
        let Some(start) = input[..end].rfind('{') else {
            return vec![input.to_string()];
        };

        let prefix = &input[..start];
        let suffix = &input[end + 1..];
        let options = &input[start + 1..end];

        let mut expanded = Vec::new();

        for opt in options.split(|c| c == '|' || c == ',') {
            let merged = format!("{}{}{}", prefix, opt, suffix);
            expanded.extend(Self::expand_alternatives(&merged));
        }

        expanded
    }
}
```

### 004: `./src/code/paths_options.rs`

```rust
// ./src/code/paths_options.rs
// przyjmowanie i obsługa opcji 
```

### 005: `./src/code/paths_patterns.rs`

```rust
// ./src/code/paths_patterns.rs
// przyjmowanie i obsługa wzorców
```

### 006: `./src/code/querypath.rs`

```rust
// ./src/code/querypath.rs
// główna logika - orkiestracja i wywołanie funkcji z innych modułów
```

### 007: `./src/lib.rs`

```rust
// ./src/lib.rs 
// querypath

// #[path = "code/querypath.rs "]
// mod ;
// pub use ::{};

#[path = "code/paths_entry.rs"]
pub mod paths_entry;

pub use paths_entry::{AnchoredPath, PathContext, PathNode, PathsEntry, normalize_path};

// #[path = "code/paths_options.rs "]
// mod ;
// pub use ::{};

// #[path = "code/paths_patterns.rs "]
// mod ;
// pub use ::{};

// #[path = "code/fs_walk.rs "]
// mod ;
// pub use ::{};


```

### 008: `./tests/_/.gh/.oiu`

```text

```

### 009: `./tests/_/.gh/aoe`

```text

```

### 010: `./tests/_/.gh/lib/dw/dwew/src/erw.wq`

```text

```

### 011: `./tests/_/.gh/lib/dw/wewe.rs`

```rust

```

### 012: `./tests/_/.gh/lib/dw/wxw`

```text

```

### 013: `./tests/_/.gh/lib/jewe.rs`

```rust

```

### 014: `./tests/_/.gh/wrt.rs`

```rust

```

### 015: `./tests/test_paths_entry.rs`

```rust
use querypath::PathsEntry;
```



2026Q3D262W38_Sat19Sep_180709623